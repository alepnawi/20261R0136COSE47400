# Paper

Build the current draft with:

```bash
latexmk -pdf panda_note.tex
```

Notes:

- `panda_note.tex` currently uses an inline boxed figure and does not require
  the files under `paper/figures/` to compile.
- The assets under `paper/figures/` are kept as editable or archival material
  for future paper revisions.
